<% page = @riderequest.nil? ? 'no_request' : 'show_request' %>
console.log("<%= @riderequest %>")
console.log("<%= page %>")
$("#drive-set-window").find(".modal-content").html("<%= j (render page) %>");
$("#drive-set-window").modal('show');
var data = <%= @response.nil? ? 'null' : @response.html_safe %>;
if(data) { showRoute(data); }
